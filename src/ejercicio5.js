// Ejercicio 5 - Conversión a segundos

let horas = 2;
let minutos = 30;

console.log(`Horas: ${horas}`);
console.log(`Minutos: ${minutos}`);

let totalSegundos = (horas * 3600) + (minutos * 60);
console.log(`Total en segundos: ${totalSegundos}`);